name: q2409-windows-repro

on:
  workflow_dispatch:
    inputs:
      mode:
        description: Workflow mode
        required: true
        default: reference
        type: choice
        options:
          - reference
          - verify
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  reproduce:
    runs-on: windows-2025
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: 3.12
      - uses: azure/setup-kubectl@v4
        with:
          version: v1.32.6
      - name: Record Windows host
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path evidence | Out-Null
          $hostInfo = [ordered]@{
            caption = (Get-CimInstance Win32_OperatingSystem).Caption
            version = (Get-CimInstance Win32_OperatingSystem).Version
            build = (Get-CimInstance Win32_OperatingSystem).BuildNumber
            runner_image = $env:ImageOS
            node_version = (node --version)
            commit_sha = $env:GITHUB_SHA
            workflow_run_id = $env:GITHUB_RUN_ID
          }
          $hostInfo | ConvertTo-Json | Set-Content -Encoding utf8NoBOM evidence/windows-host.json
      - name: Generate Reference candidate
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.mode == 'reference' }}
        shell: pwsh
        run: |
          $env:KUBECTL_PATH = (Get-Command kubectl).Source
          python ./qa/generate_reference.py
      - name: Verify attachment hashes
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'verify') }}
        shell: pwsh
        run: python ./qa/verify_hashes.py
      - name: Block kubectl external network
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'verify') }}
        shell: pwsh
        run: |
          $kubectl = (Get-Command kubectl).Source
          New-NetFirewallRule -DisplayName q2409-kubectl-no-external -Direction Outbound -Program $kubectl -Action Block | Out-Null
          Get-NetFirewallRule -DisplayName q2409-kubectl-no-external | Select-Object DisplayName,Enabled,Direction,Action | ConvertTo-Json | Set-Content -Encoding utf8NoBOM evidence/network-guard.json
      - name: Verify final task on Windows
        if: ${{ github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.mode == 'verify') }}
        shell: pwsh
        run: |
          $env:KUBECTL_PATH = (Get-Command kubectl).Source
          python ./qa/windows_verify.py
      - name: Remove kubectl network guard
        if: always()
        shell: pwsh
        run: Get-NetFirewallRule -DisplayName q2409-*-no-external -ErrorAction SilentlyContinue | Remove-NetFirewallRule
      - name: Upload independent evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: q2409-windows-evidence
          path: evidence/**
          if-no-files-found: error
          retention-days: 1
