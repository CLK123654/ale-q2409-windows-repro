# Q2409 Windows reproduction

This public repository contains one synthetic Kubernetes task and its independent Windows verification workflow. The task uses no credentials, private records, or original Lark backups.

Kubernetes operations are limited to client-side Kustomize building and local manifest parsing. No cluster control-plane behavior is claimed.
